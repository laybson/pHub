'''
Camada de repository do contexto do rulehub
'''
def get_rules_by_package(package) -> Set(RuleProto):
    # get de regras de um pacote através de sua anotação
    return rules

def get_rules_by_package_type(package_type) -> Set(RuleProto):
    # get de regras de um tipo de pacotes através de sua anotação
    return rules

# rule repository calls
# annotation repository calls
